{
  "buildCommand": "cd electricity-dashboard && pnpm build",
  "outputDirectory": "electricity-dashboard/dist",
  "framework": "vite",
  "installCommand": "corepack enable && cd electricity-dashboard && pnpm install",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
